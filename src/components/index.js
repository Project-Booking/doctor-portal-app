// src/components/index.js - Central export for all reusable components

export { Loading, SkeletonLoader, FullScreenLoading } from './Loading';
export { Button, IconButton } from './Button';
export { Input, SearchInput } from './Input';
export { Card, StatCard, InfoCard } from './Card';

export default {
  Loading,
  Button,
  Input,
  Card,
};