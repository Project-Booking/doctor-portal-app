/**
 * AppointmentHeader — legacy stub (appointments screen is now self-contained)
 * Kept for compatibility.
 */
import React from 'react';
import { View } from 'react-native';

export default function AppointmentHeader() {
  return <View />;
}